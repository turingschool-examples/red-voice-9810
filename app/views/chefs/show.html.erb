<h1><%= @chef.name %></h1>
<%= link_to "See #{@chef.name}'s Ingredients", chef_ingredients_path(@chef) %>

<div class="popular">
  <h3>Most Popular Ingredients</h3>
  <% @chef.most_popular_ingredients.each do |ing| %>
    <li><%= ing.name %></li>
  <% end %>
</div>
